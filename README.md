# "ИИ-помогатор" для баз знаний

- ✉️ Ты можешь отправлять ему свои файлы в формате: PDF, DOCX/DOC, TXT, MD.
- 📩 Поддерживаются файлы размером до 20MB.
- 📥 Информация об отправленном файле (метаданные, суммаризация и ключевые слова) будет сохранена в таблице!

---
### Ссылки

- id чат-бота: @AIKnowledgeBaseHelperBot
- [n8n workflow](https://share-n8n.net/shared/UWzv5hgau4to)
- [json backup-file](AIKnowledgeBaseHelper.json)

---

### Ход работы workflow

1. Начинается со срабатывания телеграм триггера (пользователь отправляет сообщение боту)
2. Пытаемся загрузить файл:
   - Если не удается, то обрабатывается ошибка и пользователю приходит сообщение о неправильности его действий
   - Если удается, то файл идет дальше
3. Определяем формат файла
  - Если файл DOCX/DOC формата:
    - Конвертируем в TXT с помощью CloudConvertAPI
    - Из полученного файла получаем текст с помощью официального модуля Extract from Text File
    - Полученный текст передаем дальше
  - Если файл TXT/MD формата:
    - Получаем текст с помощью Extract from Text File
    - Полученный текст передаем дальше
  - Если файл PDF формата:
    - Получаем JSON с информацией о файле с помощью Extract from PDF
    - Достаем текст из JSON
    - Полученный текст передаем дальше
  - Если у переданного файла другой формат, то выдаем сообщение о неверном формате файла
4. Удаляем лишние пробелы из начала и конца строки
5. Обрабатываем текст с помощью OpenRouter(amazon/nova-2-lite-v1:free):
  - Определяем язык текста
  - Делаем суммаризацию (3-7 предложений)
  - Выделяем ключевые слова (5-10 слов)
  - Выводим данные в формате JSON
  - При возникновении ошибки, отправляем сообщение пользователю
6. Формируем запись для Google Sheets в нужном формате
7. Добавляем запись в Google Sheets
8. Отправляем сообщение пользователю с информацией о добавленной записи

---

### Выбранные инструменты

- Amazon: Nova 2 Lite (free) для выделения ключевых слов и суммаризации, потому что:
  - Хорошо справляется с большими текстами и является бесплатной
  - Хорошо подходит ля демонстрации работы проекта
- Для преобразования DOCX/DOC в TXT используется CloudConvertAPI, так как:
  - Быстрая работа
  - Нет стандартной ноды для парсинга DOCX/DOC или конвертации DOCX/DOC в TXT
- Для парсинга PDF, TXT, MD файлов используется стандартный Extract from file, потому что:
  - Быстрая работа
  - Не требуется подключение сторонних инструментов
- Данные загруженных файлов хранятся в Google Sheets

---

### Что улучшить

- Заменить Amazon: Nova 2 Lite (free) на более подходящую новую (платную) модель (например, BART или T5)
- Внедрить OCR для распознавания сканов или изображений в документах
- Перевести тг бота на локальный сервер. Сейчас есть ограничение на размер получаемого файла (до 20MB)
- Написать свои парсеры для документов (в частности для DOCX/DOC), чтобы не ждать ответа от стороннего api

---

### Пример работы
